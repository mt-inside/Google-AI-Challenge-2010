using System.Diagnostics;
using mtBot;

namespace UnitTest
{
    //TODO: test geometry, futurePredicate
    static class PlanetTest
    {
        static public bool Test()
        {
            /* Too simple to bother with atm. Write proper tests, that build.
            Planet planet1 = new Planet( 42, Players.Singleton.Neutral, 13, 5, 6, 7 );
            Planet planet2 = new Planet( 43, Players.Singleton.Neutral, 0, 4, 9, 11 );

            Debug.Assert( planet1.Id == 42 );
            Debug.Assert( planet1.Owner == Players.Singleton.Neutral );
            Debug.Assert( planet1.ShipCount == 13 );
            Debug.Assert( planet1.GrowthRate == 5 );
            Debug.Assert( planet1.X == 6 );
            Debug.Assert( planet1.Y == 7 );
            Debug.Assert( planet1.DistanceTo( planet2 ) == 5 ); */

            return true;
        }
    }
}
